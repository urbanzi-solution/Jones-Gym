---------------------------------------------------------------------------
Task :
---------------------------------------------------------------------------

modified writeoff
> Completed
altering the route.js for insert
> Completed
adding the amount in the writeoff data 
> Completed
subtracting the sum of writeoff amount in the total balance amount 
> Completed
Write off indication in Recent_transations in profile details
> Completed
memberlist filtering in the members page
> Pending
Detail report edit and delete Membership plans and transactions table manage
> Pending
Source code is not pushed to repo

---------------------------------------------------------------------------
Files had changes :
---------------------------------------------------------------------------

src\components\Balance_form.jsx

src\components\Recent_transations.jsx

src\components\Report_Rencent_transation.jsx

src\components\Memberlist_boxes.jsx

src\app\api\write0ff\route.js

src\app\api\total_amount_recent_tans\route.js

add :

src\app\api\fetch_all_transactions\route.js

---------------------------------------------------------------------------
Task :
---------------------------------------------------------------------------

all date format change to dd/mm/yyyy

exp today

balance amount in the memberlist

remove the validation in the Renew_form.jsx

---------------------------------------------------------------------------
Files had changes :
---------------------------------------------------------------------------

src\components\Dashboard_general.jsx

src\components\Memberlist_boxes.jsx

src\components\Renew_form.jsx

---------------------------------------------------------------------------
Task :
---------------------------------------------------------------------------

---------------------------------------------------------------------------
Files had changes :
---------------------------------------------------------------------------

---------------------------------------------------------------------------


delete from transactions where bill_no = '111';

delete from transactions where user_id= 'JK124' and amount = 0;

INSERT INTO membership_plans (user_id, plan_name, bill_no, amount, discount, balance, trans_type, trainer, date, exp_date)
VALUES ('JK124', '6 M Gym', 'new123', 5000, 0, 9000, 'GPay', 'shuhaib123', CURRENT_DATE, '2026-03-26');

INSERT INTO transactions (user_id, old_bill, bill_no, plan_name, amount, balance, date, trans_type)
VALUES ('JK124', '479', '111', '1 Y Gym', 3000, 10000, CURRENT_DATE, 'GPay');

INSERT INTO transactions (user_id, old_bill, bill_no, plan_name, amount, balance, date, trans_type)
VALUES ('JK124', '479', '222', '1 Y Gym', 2000, 8000, CURRENT_DATE, 'GPay');

INSERT INTO transactions (user_id, old_bill, bill_no, plan_name, amount, balance, date, trans_type, writeoff)
VALUES ('JK124', '479', null, '1 Y Gym', 8000, 0, CURRENT_DATE, 'GPay', 'W');

SELECT 
(SUM(mp.amount) + COALESCE((SELECT SUM(t.amount) FROM transactions t), 0)) AS total_amount,
SUM(mp.discount) AS total_discount,
SUM(mp.balance) AS total_balance
FROM membership_plans mp;

SELECT 
    (SUM(mp.amount)
     + COALESCE((SELECT SUM(t.amount) FROM transactions t), 0)
    ) AS total_amount,
    SUM(mp.discount) AS total_discount,
    (
        SUM(mp.balance)
        - COALESCE(
            (SELECT SUM(t.amount)
             FROM transactions t
             WHERE t.writeoff = 'W'),
            0
        )
    ) AS total_balance
FROM membership_plans mp;




